import { Store } from "redux";
import { State } from "../reducers/dirtyHandlerIds";
import { Action, HandlerRegistry } from "@/typings";

class DragDropMonitorImpl {
  constructor(store: Store<State>, registry: HandlerRegistry) {
    
  }
}

export default DragDropMonitorImpl;
